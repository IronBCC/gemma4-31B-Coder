# Box (RTX 6000 Pro Blackwell) deps for Phase 0/A/B. Pin exact versions per your CUDA/vLLM.
# --- serving / inference ---
vllm>=0.6.0                 # confirm version that supports Gemma-4 + NVFP4 + eagle3 spec config
# --- agent harness ---
mini-swe-agent             # same harness for collection + eval (confirm package/repo name)
# --- data / eval ---
datasets>=2.19
huggingface-hub>=0.23
swebench                   # official SWE-bench harness (Docker-based scoring)
# --- utils ---
docker
pyyaml
tqdm
openai>=1.30                # OpenAI-compatible client for the vLLM endpoint (agent loop)
tree-sitter                 # (optional) multi-lang ast_edit backend — TODO
pytest                      # scaffold unit tests
# --- phase I DeepSpeed/Ulysses long-context probe ---
torch                       # install CUDA build matching the box driver
transformers
peft
deepspeed
# optional tree-sitter grammars for ast_edit (install only the langs you target)
tree-sitter-java
tree-sitter-kotlin
tree-sitter-rust
tree-sitter-cpp
tree-sitter-go
tree-sitter-typescript
tree-sitter-javascript
